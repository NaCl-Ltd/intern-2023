<% provide(:title, t("Likes-ranking")) %>
<h1><%= t("Likes-ranking") %></h1>
<div class="row">
  <div class="col-md-8">
    <% @sorted_likes = Like.group(:micropost_id).count.sort_by { |_, count| -count }.first(10) %>
    <%= render 'shared/ranking', likes: @sorted_likes %>
  </div>
</div>
