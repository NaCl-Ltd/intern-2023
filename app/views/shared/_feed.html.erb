<% if @feed_items.any? %>
  <ol class="microposts">
    <% if @fixed_item.present? %>
      <%= render @fixed_item %>
    <% end %>
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
<% end %>
